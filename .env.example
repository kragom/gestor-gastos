# Copia este fichero a .env para desarrollo local (NO lo subas a git/HF).
SECRET_KEY=cambia-esto-por-una-cadena-larga-aleatoria
# Persistencia opcional en HF Dataset privado:
HF_TOKEN=
HF_DATASET_REPO=hectorpc19/balance-db
HF_DB_FILENAME=balance.db
SYNC_DEBOUNCE_SECONDS=4
DEFAULT_CURRENCY=EUR
